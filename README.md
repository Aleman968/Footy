# Footy App

## Avvio locale
```bash
py -m pip install -r requirements.txt
py -m streamlit run app.py
```

## Streamlit Cloud
Secrets:
```toml
FOOTYSTATS_API_KEY = "LA_TUA_CHIAVE"
```

## Nota
OVER 2.5 viene confermato solo se i 3 risultati esatti più probabili sono tutti coerenti con over 2.5.
